# ── Core engine (regex + spaCy + strategies) ────────────────────────────────
spacy==3.8.14
Faker==40.21.0
PyYAML==6.0.3
 
# ── Presidio engine ──────────────────────────────────────────────────────────
presidio-analyzer==2.2.362
presidio-anonymizer==2.2.362
 
# ── Ensemble engine (TightEnsembleEngine) ───────────────────────────────────
torch==2.12.0
transformers==4.57.6
spacy-huggingface-pipelines==0.0.4
 
# ── REST API (deidentifier/api.py) ──────────────────────────────────────────
fastapi>=0.110.0
pydantic==2.13.4
uvicorn>=0.29.0
 
# ── Dev / testing ────────────────────────────────────────────────────────────
pytest>=7.0
pytest-cov>=4.0
 
# ── Evaluation (eval/evaluate_ensemble.py) ──────────────────────────────────
kaggle>=1.6
 
# ── Post-install: download spaCy models ─────────────────────────────────────
# python -m spacy download en_core_web_sm    # ensemble engine (lightweight)
# python -m spacy download en_core_web_trf   # default + presidio engine (transformer)